apiVersion: v1
kind: Service
metadata:
  name: ai-canary-demo
  namespace: ai-canary
  labels:
    app: ai-canary-demo
spec:
  selector:
    app: ai-canary-demo
  ports:
    - name: http
      port: 80
      targetPort: 5000
  type: ClusterIP